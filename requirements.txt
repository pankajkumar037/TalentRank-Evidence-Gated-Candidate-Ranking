# TalentRank — pinned dependencies
#
# The RANKING step (rank.py) needs only orjson + numpy: it loads precomputed
# embeddings and does cosine, no model inference, no network. sentence-transformers
# and torch are used ONLY by the offline precompute_embeddings.py step.

# --- ranking step (required, lightweight, CPU-only, no network) ---
orjson==3.11.3
numpy==2.2.6

# --- offline pre-computation of embeddings (not part of the 5-min budget) ---
sentence-transformers==5.3.0
torch==2.9.1

# --- sandbox UI (app.py) ---
streamlit==1.52.1

# --- tests ---
pytest==8.3.4
