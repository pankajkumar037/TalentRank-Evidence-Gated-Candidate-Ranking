# TalentRank — core dependencies (ranking + sandbox + tests).
#
# The RANKING step (rank.py) and the Streamlit sandbox (app.py) run on these alone —
# no torch, no sentence-transformers, no network. This is also what Streamlit
# Community Cloud installs when hosting the sandbox, so the deploy stays lightweight.
#
# The offline embedding experiment (precompute_embeddings.py, the excluded --semantic
# booster) needs the heavier deps in requirements-embeddings.txt — install that only
# if you want to reproduce that A/B finding. It is NOT needed to produce the submission.

orjson==3.11.3
numpy==2.2.6
openpyxl==3.1.5      # submission.xlsx generation (rank.py / make_xlsx.py)
streamlit==1.52.1    # sandbox UI (app.py)
pytest==8.3.4        # adversarial test suite
