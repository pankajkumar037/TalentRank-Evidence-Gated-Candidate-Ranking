# Large challenge data — regenerate with `python extract_bundle.py`
data/candidates.jsonl
data/candidates.jsonl.gz

# Challenge dataset bundle — do NOT redistribute (contains the full candidate pool)
*.zip

# Blank org-provided idea template (our filled deck is TalentRank_IdeaSubmission.pptx)
IdeaSubmissionTemplate.pptx

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Scratch outputs
submission_*.csv

# NOTE: the final ranker is PURE RULES and needs no artifacts. artifacts/*.npy are
# only for the excluded `--semantic` experiment; keep them to reproduce that A/B
# finding, or delete and regenerate with `python precompute_embeddings.py`.

# Office lock/temp files
~$*
*.tmp
